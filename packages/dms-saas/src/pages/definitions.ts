/**
 * Page and module descriptors. The interface package owns them so a consuming
 * module can read the same ids and slugs this module registers.
 */
export * from "@antelopejs/interface-dms-saas/page-definitions";
