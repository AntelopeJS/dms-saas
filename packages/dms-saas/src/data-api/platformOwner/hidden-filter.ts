/**
 * Hidden string filter decorator. The interface package owns it so consuming
 * modules register the same filter shape on their own data controllers.
 */
export * from "@antelopejs/interface-dms-saas/hidden-filter";
