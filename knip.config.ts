import { antelopeKnipConfig } from "@antelopejs/tooling-configs/knip";

/** Re-exported so a package config reaches the preset through the root. */
export { antelopeKnipConfig };

export default antelopeKnipConfig();
